{
  "name": "lift-ecommerce",
  "version": "1.0.0",
  "description": "LIFT e-commerce platform (Express + MySQL backend, static frontend in App/)",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "nodemon server/index.js",
    "db:init": "mysql -u root -p < database/schema.sql",
    "db:seed": "mysql -u root -p < database/seed.sql"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.11.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
